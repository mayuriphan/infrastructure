crete bootstrap and run below locally to setup first s3 bucket and stuff
terraform workspace new development and so on
terraform init
terraform apply -var="environment=development"  and so on..
#for later use
terraform workspace select development   
terraform plan -var="environment=development" -out=tfplan
terraform apply "tfplan=dev"  and so on, make sure to change out files "tfplan-prod" for diff env and rhen apply 
---------------
wget -O - https://apt.releases.hashicorp.com/gpg | sudo gpg --dearmor -o /usr/share/keyrings/hashicorp-archive-keyring.gpg
echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/hashicorp-archive-keyring.gpg] https://apt.releases.hashicorp.com $(grep -oP '(?<=UBUNTU_CODENAME=).*' /etc/os-release || lsb_release -cs) main" | sudo tee /etc/apt/sources.list.d/hashicorp.list
sudo apt update && sudo apt install terraform